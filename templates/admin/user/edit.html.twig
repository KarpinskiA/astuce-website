{% extends 'base.html.twig' %}

{% block title %}Modification de l'utilisateur : {{ user.username }}{% endblock %}

{% block body %}
    <nav class="mb-4">
        <a href="{{ path('app_admin_user_index') }}" class="text-primary text-decoration-none">
            <i class="bi bi-arrow-left"></i> Retour dashboard utilisateur
        </a>
    </nav>

    <h1>Modification de l'utilisateur : {{ user.username }}</h1>

    {{ include('admin/user/_form.html.twig', {
            'button_label': 'Mettre à jour', 
            'button_class': 'btn-secondary mb-2'
        }) 
    }}

    {{ include('admin/user/_delete_form.html.twig') }}
{% endblock %}
